# temp-logger ICM

## Purpose

This repository is an ICM project instance (systems-engineering template, base
variant) for **temp-logger** — a small CLI utility that appends temperature
sensor readings to a timestamped CSV log.

## Agent Instructions

1. Read `ICM.md` (this file)
2. Read top-level `CONTEXT.md` and route into the correct workspace
3. Load local workspace context where present
4. Preserve engineering traceability (every change links to a REQ ID)
5. Operate only within workspace scope

## Escalation Conditions (Global)

Escalate to the human operator when:
- Requirements conflict or are ambiguous
- A dependency is unresolved and blocking progress
- A task does not match any row in the routing table

## Version Control

See the Version Control section of `CONTEXT.md`. Never commit credentials.
